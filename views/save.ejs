<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <title>Saved Songs</title>
  <link rel="stylesheet" type="text/css" href="/css/style.css">
  <link rel="stylesheet" type="text/css" href="/css/normalize.css">
  <link href="https://fonts.googleapis.com/css?family=Alice|PT+Sans+Caption|Pacifico|Quattrocento+Sans" rel="stylesheet">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.0/jquery.min.js"></script>
  <script type="text/javascript">

  //got this function from: http://papermashup.com/read-url-get-variables-withjavascript/
$( document ).ready(function() {

function getUrlVars() {
var vars = {};

var parts = window.location.href.replace(/[?&]+([^=&]+)=([^&]*)/gi, function(m,key,value) {
vars[key] = value;
});
return vars;
}

var track = getUrlVars()['track'];
var artist = getUrlVars()['artist'];
// $('.one').append(track);

// $('.two').append(artist);

//i got this function from: https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String/split
function splitString(stringToSplit, separator) {
  var arrayOfStrings = stringToSplit.split(separator);


  return arrayOfStrings.join(' ');
}



var plus = '+';
var art = splitString(artist, plus);
var tra = splitString(track, plus);
$('.one').append(track).text(tra);
$('.two').append(artist).text(art);

});

  </script>
</head>
<body>
<form class="logout" action="/users/profile"  accept-charset="utf-8">
      <input type="Submit" Value="Home" class="color"></a>

</form>
<br>
    <h2>Encyc-lyric-pedia</h2>

    <p><%= user.username %>'s Liked Songs:</p>

<div class="like-container">
Song:
<h6 class="one"> </h6>
Artist:
<h6 class="two"> </h6>
</div>
<form action="/save/likes/<%= lyrics._id %>?_method=DELETE" method="post">
          <input type="submit" value="Delete" class="butt">
</form>
        <!-- <form action="/save/<%= lyrics._id %>?_method=DELETE" method="post">
          <input type="submit" value="Delete">
        </form> -->

  </div>

</body>
</html>
